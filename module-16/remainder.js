var oragePrice = 22;
var person = 5 ;

var vagFol = oragePrice % person; // using for know vagfoll

console.log(vagFol);

var number1= 0.1;
var number2 = 0.2;

var totalNumber = number1 + number2;
console.log(totalNumber) 

// using don't show multiple decimal number 

totalNumber = totalNumber.toFixed(1);

console.log(parseFloat(totalNumber)) // using string to number covert 
